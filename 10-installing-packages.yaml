---
- name: "Installing packages"
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: "Package names"
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql-server
